# Historical import v3

This document is superseded by `HISTORICAL_IMPORT_V4.md`.

Do not use the original v3 branch lookup that assumed workbook aliases `PBL`, `SKY`, and `RCK` were database `branch.code` values. In production they are source aliases and must be mapped to the exact existing branch names.
